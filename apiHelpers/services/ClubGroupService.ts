
import ClubGroup from '../entities/ClubGroup';
// import clubGroupRepository from '../repositories/ClubGroupRepository'

class ClubGroupService {
  // async create(name: string): Promise<ClubGroup> {
  //   try {
  //     const clubGroupId = await clubGroupRepository.create(name);

  //     return { id: clubGroupId, name }
  //   } catch (err) {
  //     throw err;
  //   }
  // }
}

export default new ClubGroupService()
